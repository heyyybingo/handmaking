# ============================================================
# npm 配置
# ============================================================

# 镜像源（淘宝镜像，国内加速）
registry=https://registry.npmmirror.com/

# 严格引擎版本检查
engine-strict=true

# 允许旧版 peer dependencies（解决依赖冲突）
legacy-peer-deps=true